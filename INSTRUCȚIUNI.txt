# 📦 Update BioSync Pro

Acest folder conține DOAR fișierele modificate/noi.

## Instrucțiuni:

### 1. Copiază fișierele în proiectul tău:

| Fișier din acest folder | Unde îl pui |
|------------------------|-------------|
| `index.html` | înlocuiește `biosync-pro/index.html` |
| `vercel.json` | copiază în `biosync-pro/` (fișier NOU) |
| `public/sw.js` | copiază în `biosync-pro/public/` (fișier NOU) |
| `public/manifest.json` | copiază în `biosync-pro/public/` (fișier NOU) |
| `public/icons/` (tot folderul) | copiază în `biosync-pro/public/` (folder NOU) |
| `src/App.jsx` | înlocuiește `biosync-pro/src/App.jsx` |

### 2. În terminal (VS Code):

```bash
git add .
git commit -m "Add PWA, disclaimer, copyright and smart caching"
git push
```

### 3. Gata! 🎉

Vercel va face deploy automat.
